services:
  powerdown-controller:
    build:
      context: .
      args:
        VERSION: ${POWERDOWN_CONTROLLER_VERSION:-dev}
    container_name: powerdown-controller
    env_file:
      - .env
    volumes:
      - ./state:/state
    restart: unless-stopped
