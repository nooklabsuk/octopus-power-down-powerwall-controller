services:
  powerdown-controller:
    build: .
    container_name: powerdown-controller
    env_file:
      - .env
    volumes:
      - ./state:/state
      - ./tariff:/tariff:ro
    restart: unless-stopped
